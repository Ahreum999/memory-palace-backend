[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install requests feedparser beautifulsoup4 python-dotenv"
]

[start]
cmd = "npm start"